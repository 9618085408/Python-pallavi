import re

def has_special_characters(string: str) :
    pattern = r'[^A-Za-z0-9 ]'

    
    if re.search(pattern, string):
        return True
    else:
        return False

print(has_special_characters("Hello World!"))  
print(has_special_characters("HelloWorld"))   
