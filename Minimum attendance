MINIMUM_ATTENDANCE = 75

name = input("Enter the student's name: ")
attendance = float(input("Enter the student's attendance percentage: "))

if attendance >= MINIMUM_ATTENDANCE:
    print(f"{name} is allowed to sit for the exam.")
else:
    print(f"{name} is not allowed to sit for the exam because their attendance is below {MINIMUM_ATTENDANCE}%.")
